type Query {
  me: User
  user(id: ID!): User!
  users: [User!]!
  peak(id: ID!): Peak!
}

type Mutation {
  updateUser(input: UpdateUserInput!): User!
}

type User {
  id: ID!
  email: String!
  displayName: String!
  city: String!
  contact: String!
}

type Peak {
  id: ID!
  name: String!
  longitude: Float!
  latitude: Float!
  absHeight: Int!
  description: String!
  mountainRange: String!
}

input UpdateUserInput {
  id: ID!
  displayName: String!
  city: String!
  contact: String!
}
